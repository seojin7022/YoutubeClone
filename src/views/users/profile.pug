extends ../base
include ../mixins/video

block content 
    div.profile_data
        img(src="/" + user.avatarUrl).avatar
        h3=user.username
    div.video_grid
        each video in user.videos
            +video(video)
        else
            span.empty_message There is no video